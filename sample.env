# Environment Configuration
# Copy this file to .env.development for local development
# For production, use .env.production or set variables in Vercel

NODE_ENV=development
PORT=3000

# Hugging Face API Token (Required)
# Get one at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Hugging Face Configuration
HF_MODEL=meta-llama/Llama-3.1-8B-Instruct:novita
HF_API_URL=https://router.huggingface.co/v1/chat/completions
HF_MAX_TOKENS=100
HF_TEMPERATURE=0.95

# Supabase Configuration
# Get these from your Supabase Project Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# CORS Origins (comma separated)
# Development: Optional (allows all origins if not set)
# Production: Required (specify your frontend domain(s))
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Debug Logging (uses 'debug' npm package)
# Set to 'true', '1', or '*' to enable all logs
# Set to specific namespace like 'kitchen-center:stats' to enable only stats logs
# Leave unset or set to false to disable all logs
DEBUG=false
